"""
Introdução ao módulo Unittest

Unittest -> Testes Unitários

O que são testes unitarios?
R: Teste unitario é a forma de se testar unidades individuais de codigo fonte.

unidades individuais podem ser: funções, métodos, classes, módulos etc.

OBS: Teste unitário não é específico da linguagem Python.


TestCase -> Casos de teste
"""

# Pratica utilizando TDD

"""
Método                  Checa que

assertEqual(a, b)       a==b
assertNotEqual(a, b)    a!=b
assertTrue(x)           x é verdadeiro
assertFalse(x)          x é falso
assertIs(a, b)          a é b
assertIsNot(a, b)       a não é b
assertIsNone(x)         x é None
assertIsNotNone(x)      x não é None
assertIn(a, b)          a está em b
assertNotIn(a, b)       a não está em b
assertIsInstance(a, b)    a é instância de b
assertNotIsInstance(a, b)    a é não é instância de b

OBS: Por convenção, todos os testes em um test case, devem ter seu nome iniciado com test_ 





"""